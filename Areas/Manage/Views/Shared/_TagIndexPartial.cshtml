@model PageNatedList<Tag>
<table class="table table-bordered table-striped ">
    <thead>
        <tr>
            <th>No</th>
            <th>Name</th>
            <th>Settings</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Tag tag in Model)
        {
            <tr>
                <td>@tag.Id</td>
                <td>@tag.Name</td>
                
                <td>
                    <a asp-area="manage" asp-controller="tag" asp-action="update" asp-route-id="@tag.Id" class="btn btn-warning">Update</a>
                    @if (tag.IsDeleted)
                    {
                        <a asp-area="manage" asp-controller="tag" asp-action="restore" asp-route-id="@tag.Id" asp-route-status="@ViewBag.Status" class="btn btn-info restoreBtn">Restore</a>

                    }
                    else
                    {
                        <a asp-area="manage" asp-controller="tag" asp-action="delete" asp-route-id="@tag.Id" asp-route-status="@ViewBag.Status" class="btn btn-danger deleteBtn">Delete</a>

                    }

                </td>
            </tr>
        }
    </tbody>
</table>